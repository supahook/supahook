class Auth { // no db
    // type: 'API_KEY', 'BASIC', 'BEARER', 'HMAC', 'OAUTH', "MTLS"

    // IP whitelistening: boolean

    // Rate Limit :

    // Security : boolean

    // authLocation: QUERY, HEADERS

    // authName:

    // authValue: 
}