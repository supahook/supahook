class Webhook { // db
    // id
    //auth : WebhookAuth

    // method: to create webhook  it can be overloaded
    // method: to save webhook  it can be overloaded
    // class method: to get all Webhooks, it can be overloaded
    // method to get current webhook calls, it can be overloaded
}